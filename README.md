## Local setup

```bash
npm install
```


## Running the Docker with Mongo service:

```bash
# for start
docker-compose up -d

# for end
docker-compose down
```


## Running the backend service:

```bash
npm run start:dev
```
